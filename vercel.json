{
  "buildCommand": "npm ci && prisma generate && rm -rf .next && next build",
  "env": {
    "SKIP_ENV_VALIDATION": "true"
  },
  "functions": {
    "api/**": {
      "memory": 3008,
      "timeout": 30
    }
  }
}
