// Path: src/app/owner/messages/page.tsx
// Owner messages uses identical conversation UI as student dashboard.
// The conversations API returns all convos where the user is a participant,
// so both student and owner see their respective threads automatically.
export { default } from "@/app/dashboard/messages/page";
